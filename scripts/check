#!/usr/bin/env bash
#
# Build the repository's single local verification entry point here.
#
# Your checks may call any tools or additional scripts you choose, but this
# command must:
#   - run from the repository root inside the Dev Container;
#   - exercise the application at TARTAN_BASE_URL, which defaults to
#     http://localhost:8636;
#   - assume the Compose application is already running; do not build, start,
#     stop, or reset it;
#   - include the inherited tests and every test you retain or add;
#   - return a nonzero status when any check fails;
#   - print enough information to identify a failure; and
#   - be repeatable and finish within five minutes.
#
# Add verification commands below this line.

set -euo pipefail

export TARTAN_BASE_URL="${TARTAN_BASE_URL:-http://localhost:8636}"

npm test
