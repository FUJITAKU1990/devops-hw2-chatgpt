[extend]
useDefault = true

# Exclude this file itself from scanning to avoid self-matching the allowlist entries below
[[allowlists]]
paths = ['''^\.gitleaks\.toml$''']

[[allowlists]]
description = "Known-harmless fixed test value: throwaway password used only by tests/hw2-shared-purchase-contract.test.ts to register disposable test accounts against a local stack; not tied to any real credential."
regexTarget = "match"
regexes = [
  '''Hw2Passw0rd!''',
]
