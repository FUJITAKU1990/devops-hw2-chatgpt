# System Tests

Tests in this directory exercise the running Tartan Tickets application through its public ingress.
They use `TARTAN_BASE_URL`, which defaults to `http://localhost:8636`, and run as part of root-level `./scripts/check`.
